name: Pre-commit Hooks Runner

on:
  pull_request:
  push:
    branches:
      - "*"

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v4

      - name: Execute pre-commit
        uses: pre-commit/action@v3.0.1
